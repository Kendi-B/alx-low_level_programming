#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

/**
 * positive_or_negative checks whether i is positive or negative
 */
void positive_or_negative(int i);

/**
 * int largest_number returns largest number of the three
 */
int largest_number(int a, int b, int c);

/**
 * int convert_day converts day of the month to day of the year
 */
int convert_day(int month, int day);

/**
 * print_remaining_days prints remaining days of the year
 */
void print_remaining_days(int month, int day, int year);
#endif /* MAIN_H */
